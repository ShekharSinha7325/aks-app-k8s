apiVersion: v1
kind: Service

metadata:
  name: aks-app-frontend
  namespace: aks-app
  labels:
    app: aks-app-frontend
    component: frontend

spec:
  type: ClusterIP

  selector:
    app: aks-app-frontend

  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: http