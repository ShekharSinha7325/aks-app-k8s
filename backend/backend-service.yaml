apiVersion: v1
kind: Service

metadata:
  name: aks-app-backend
  namespace: aks-app
  labels:
    app: aks-app-backend

spec:
  type: ClusterIP

  selector:
    app: aks-app-backend

  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: http